projection implementation in class ZBP_C_DJS_LANG unique;
strict ( 2 );
use draft;
define behavior for ZC_DJS_LANG alias Language
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action TranslateText;
}