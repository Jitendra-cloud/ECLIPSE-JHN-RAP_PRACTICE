projection;
strict ;
use draft;

define behavior for ZCJS_C_RapPartner alias Partner
{
  use create;
  use update;
  use delete;

  use action fillEmptyStreets;
  use action clearAllEmptyStreets;
  use action fillEmptyStreets_P;
  use action copyLine;
  use action withPopu;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
  use action Resume;
}