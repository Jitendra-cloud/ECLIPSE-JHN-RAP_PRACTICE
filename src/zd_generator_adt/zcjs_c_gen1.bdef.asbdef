projection implementation in class ZBP_C_DJS_GEN1 unique;
strict ( 2 );
use draft;
define behavior for ZCJS_C_GEN1 alias Gen1
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}